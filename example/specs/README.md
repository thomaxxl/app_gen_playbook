# Example Specs

This directory preserves the filled airport-specific artifact set that was
previously stored under the top-level `specs/` tree.

Use it as a concrete historical example, not as the generic template source for
new runs.
