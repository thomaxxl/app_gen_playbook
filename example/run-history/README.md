# Example Run History

This directory preserves historical run-local files that were archived when the
playbook was reset to a neutral state.

It exists so the playbook can keep a clean `runs/current/` while still
retaining the prior airport run narrative.
