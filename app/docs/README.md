# App Docs

Accepted run-specific artifacts MAY be copied here after product acceptance.

Suggested structure:

- `product/`
- `architecture/`
- `ux/`
- `backend-design/`

Do not treat this directory as the active drafting area. During a run, agents
MUST work in `../../runs/current/artifacts/`.
