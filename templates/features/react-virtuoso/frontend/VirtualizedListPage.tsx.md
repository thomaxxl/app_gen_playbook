# `VirtualizedListPage.tsx`

Use this snippet only for list surfaces where virtualization is justified by
data volume.

Rules:

- define explicit empty and loading states
- keep filtering and search behavior visible
- do not replace ordinary React-admin lists without a documented reason
