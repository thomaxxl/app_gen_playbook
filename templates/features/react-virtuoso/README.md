# React Virtuoso Feature Templates

Load this template pack only when `react-virtuoso` is enabled and assigned to
Frontend in the run load plan.

Expected implementation steps:

1. read `../../../specs/features/react-virtuoso/README.md`
2. read `../../../specs/features/react-virtuoso/frontend.md`
3. add `react-virtuoso@4.18.3`
4. use the feature-owned virtualized page wrapper only for genuinely large data sets
