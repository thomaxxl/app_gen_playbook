# `SortableList.tsx`

Use this only for explicit reorder or drag/drop surfaces.

Rules:

- define keyboard reorder behavior
- define persistence after reorder
- do not silently change generic CRUD list behavior
