# dnd-kit Feature Templates

Load this template pack only when `dnd-kit` is enabled and assigned to
Frontend in the run load plan.

Expected implementation steps:

1. read `../../../specs/features/dnd-kit/README.md`
2. read `../../../specs/features/dnd-kit/frontend.md`
3. add `@dnd-kit/react@0.3.2` and `@dnd-kit/helpers@0.3.2`
4. keep drag/drop behavior local to the approved page or component
