# `FlowCanvasPage.tsx`

Use this only for explicit graph or workflow-canvas pages.

Rules:

- import `@xyflow/react/dist/style.css`
- define controlled or uncontrolled state ownership
- keep the canvas off `Home` and generic CRUD pages unless the run explicitly enables it
