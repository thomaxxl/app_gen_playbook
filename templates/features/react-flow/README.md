# React Flow Feature Templates

Load this template pack only when `react-flow` is enabled and assigned to
Frontend in the run load plan.

Expected implementation steps:

1. read `../../../specs/features/react-flow/README.md`
2. read `../../../specs/features/react-flow/frontend.md`
3. add `@xyflow/react@12.10.1`
4. import `@xyflow/react/dist/style.css` in the feature-owned wrapper
