# Lexical Editor Feature Templates

Load this template pack only when `lexical-editor` is enabled and assigned in
the run load plan.

Expected implementation steps:

1. read `../../../specs/features/lexical-editor/README.md`
2. read `../../../specs/features/lexical-editor/frontend.md`
3. add the approved Lexical package set at one exact version line
4. confirm backend storage and read-only rendering policy before copying the editor snippet
