# `MotionReveal.tsx`

Use this only inside pages or sections explicitly approved for Motion-based
transitions.

Rules:

- import from `motion/react`
- keep reduced-motion behavior explicit
- do not add this wrapper to generic CRUD controls by default
