# Embla Carousel Feature Templates

Load this template pack only when `embla-carousel` is enabled and assigned to
Frontend in the run load plan.

Expected implementation steps:

1. read `../../../specs/features/embla-carousel/README.md`
2. read `../../../specs/features/embla-carousel/frontend.md`
3. add `embla-carousel-react@8.6.0`
4. add autoplay only when the run explicitly enables it
