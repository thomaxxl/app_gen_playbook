# `CarouselSection.tsx`

Use this only for exceptional content-driven surfaces such as a media gallery
or proof carousel.

Rules:

- keep explicit previous and next controls
- do not enable autoplay by default
- prefer a static grid when a carousel is not clearly justified
