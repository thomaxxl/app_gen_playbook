# Frontend Adapter Exception Record

Use this whenever the frontend proposes behavior outside the normal `safrs-jsonapi-client` lane.

## Exception id
- `FE-ADAPTER-EXC-...`

## Context
- resource(s):
- screen(s):
- user-visible behavior:
- related backend endpoint(s):

## Rejected canonical lane
- package feature considered:
- why the normal package lane was not sufficient:
- why a thin package wrapper was not sufficient:

## Proposed replacement behavior
- local file(s):
- runtime boundary:
- does it still return package-compatible records? yes/no
- does it still preserve `tab_groups` semantics? yes/no
- does it still preserve include hydration? yes/no
- does it still use the dataProvider boundary? yes/no

## Risk
- divergence risk:
- maintenance risk:
- rollback plan:
- upstream contribution or convergence target:

## Proof required
- include proof:
- relationship-route proof:
- search-wrapper proof:
- write-flow proof:
- no-direct-fetch proof:
