# Uploads Feature Summary

This is the only fully supported optional feature pack today.

Load it only when the capability profile enables `uploads` and the load plan
assigns uploads work to the current role.

It controls:

- file metadata resources
- upload-aware frontend/backend integration
- logical media routes
- uploads-specific validation

Full docs:

- `../../../specs/features/uploads/README.md`
