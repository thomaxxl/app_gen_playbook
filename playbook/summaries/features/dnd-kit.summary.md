# dnd-kit Feature Summary

This is an experimental optional feature pack.

Load it only when the capability profile enables `dnd-kit`.

It controls drag/drop and reorderable interfaces that are explicitly enabled
for a run.

Full docs:

- `../../../specs/features/dnd-kit/README.md`
