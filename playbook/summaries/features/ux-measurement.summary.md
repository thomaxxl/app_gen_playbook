# UX Measurement Feature Summary

This is an experimental optional feature pack.

Load it only when the capability profile enables `ux-measurement`.

It controls optional measurement/instrumentation expectations only. It does
not imply shipping analytics by default.

Full docs:

- `../../../specs/features/ux-measurement/README.md`
