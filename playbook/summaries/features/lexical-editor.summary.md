# Lexical Editor Feature Summary

This is an experimental optional feature pack.

Load it only when the capability profile enables `lexical-editor`.

It controls rich-text editing and requires explicit frontend plus backend
storage policy.

Full docs:

- `../../../specs/features/lexical-editor/README.md`
