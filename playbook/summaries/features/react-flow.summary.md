# React Flow Feature Summary

This is an experimental optional feature pack.

Load it only when the capability profile enables `react-flow`.

It controls node-based editors, workflow canvases, and graph-like custom
views.

Full docs:

- `../../../specs/features/react-flow/README.md`
