# Font Awesome Icons Feature Summary

This is a supported optional feature pack.

Load it only when the capability profile enables `font-awesome-icons`.

It controls the visible app-facing icon system through the `AppIcon` wrapper.

Full docs:

- `../../../specs/features/font-awesome-icons/README.md`
