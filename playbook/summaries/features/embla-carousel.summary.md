# Embla Carousel Feature Summary

This is an experimental optional feature pack.

Load it only when the capability profile enables `embla-carousel`.

It controls exceptional carousel or slider surfaces, not ordinary CRUD pages.

Full docs:

- `../../../specs/features/embla-carousel/README.md`
