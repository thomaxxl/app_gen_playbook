# Embla Carousel Frontend Guidance

Feature-owned code MUST define:

- accessible previous and next controls
- slide-announcement behavior when needed
- responsive and touch behavior
- when a static grid is preferred over a carousel

Carousel usage MUST NOT creep into normal CRUD pages by default.
