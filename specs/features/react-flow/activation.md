# React Flow Activation

Enable this pack only when the run explicitly needs:

- workflow canvases
- pipeline editors
- topology or dependency diagrams
- node-based editing surfaces

Install pin:

- `@xyflow/react@12.10.1`

Feature-owned code MUST import:

- `@xyflow/react/dist/style.css`
