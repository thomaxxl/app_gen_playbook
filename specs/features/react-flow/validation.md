# React Flow Validation

Required when this feature is enabled:

- one smoke test for canvas rendering
- one add/remove node or edge test
- one persistence round-trip test
- one responsive layout or viewport-behavior test
