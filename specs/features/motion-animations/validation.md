# Motion Animations Validation

Required when this feature is enabled:

- one reduced-motion-safe component test
- one browser smoke path proving no runtime or hydration errors
- one UX check proving the page remains readable and actionable when motion is reduced
