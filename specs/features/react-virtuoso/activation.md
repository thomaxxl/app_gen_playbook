# React Virtuoso Activation

Enable this pack only when the run has unusually large list, table, grid, or
feed surfaces where normal rendering would be too heavy.

Install pin:

- `react-virtuoso@4.18.3`

This pack MUST NOT be enabled for ordinary React-admin list pages with modest
row counts.
