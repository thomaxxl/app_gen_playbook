# React Virtuoso Validation

Required when this feature is enabled:

- one large-dataset rendering test that proves the DOM does not fully explode
- one keyboard or scroll-behavior test
- one search or filter behavior test inside the virtualized view
