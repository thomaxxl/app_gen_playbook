# Lexical Editor Validation

Required when this feature is enabled:

- one edit/save/reload round-trip test
- one read-only render test
- one paste or import test when HTML or Markdown support is enabled
- one accessibility smoke path for toolbar and editor focus behavior
