# dnd-kit Frontend Guidance

Feature-owned code MUST define:

- keyboard accessibility behavior
- reorder persistence contract
- pointer and touch sensor policy
- collision or overlay strategy for the chosen surface

This pack MUST remain opt-in per page or component. It MUST NOT silently
change generic CRUD interaction behavior.
