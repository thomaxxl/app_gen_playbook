# Role State

This directory contains the mutable runtime state for the active run.

Each role directory contains:

- `inbox/`
- `processed/`
- `context.md` once that role has executed

Static role definitions remain under `playbook/roles/`.
