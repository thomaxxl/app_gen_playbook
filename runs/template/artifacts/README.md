# Artifact Template Root

This directory exists only to define the neutral starter shape for run-owned
artifacts.

Populate local `runs/current/artifacts/` from the generic spec templates when
the run begins.
