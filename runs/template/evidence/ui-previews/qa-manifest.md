# QA Screenshot Manifest

starter_status: pending-review-evidence

Replace this starter note during Phase 8.

Required fields:

- `capture_status: captured`
- a `## Story Screenshot Coverage` markdown table with columns:
  `Story ID | Supporting Surface IDs | Screenshot Files | Coverage Status | Notes`

The QA screenshot manifest is not complete until it maps required story IDs to
the saved screenshot files that support the tested UI surfaces.
